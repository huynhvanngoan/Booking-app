import React from 'react'

const AuthModal = ({ isOpen, onOpenChange }: {isOpen:boolean; onOpen?:() => void; onOpen}) => {
  return (
    <div>
      AuthModal
    </div>
  )
}

export default AuthModal
