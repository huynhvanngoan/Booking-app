import React from 'react'

const AuthModal = ({ isOpen, onOpenChange }: {isOpen:boolean; onOpen?:() => void; onOpenChange}) => {
  return (
    <div>
      AuthModal
    </div>
  )
}

export default AuthModal
